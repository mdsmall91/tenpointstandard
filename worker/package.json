{
  "name": "tenpoint-model-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side proxy holding the Anthropic API key for tenpointstandard.com",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
